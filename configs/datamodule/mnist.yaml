# MNIST DataModule configuration
# Uses PyTorch's built-in MNIST dataset

_target_: src.datamodules.mnist_datamodule.MNISTDataModule

data_dir: ${paths.data_dir}
batch_size: 64
num_workers: 4
pin_memory: True
persistent_workers: False